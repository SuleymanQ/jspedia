/**
 * ```ts
 * ```
 * @description The Accessible method determines if the given value is array accessible
 * @since v0.0.42
 *
 */
export function accessible(arr) {
    return Array.isArray(arr);
}
